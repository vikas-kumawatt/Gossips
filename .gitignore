node_modules/
.env
*.env.*

# Python bytecode. Regenerated on every import, differs per interpreter version, and four of
# these were tracked — so any test run showed up as a binary diff nobody could review.
__pycache__/
*.py[cod]

# Firebase service account private keys
*-firebase-adminsdk-*.json

.claude
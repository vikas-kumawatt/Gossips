const NoDataMessage = ({ message }) => {
  return (
    <p className="text-center w-full p-4 rounded-full bg-neutral-900">
      <span>{message}</span>
    </p>
  );
};
  
export default NoDataMessage
  